[auth]
provider.url=${env:AUTH_PROVIDER_URL}
provider.realm=${env:AUTH_PROVIDER_REALM}
client.id=${env:AUTH_CLIENT_ID}
user.username=${env:AUTH_USER_USERNAME}
user.password=${env:AUTH_USER_PASSWORD}

[union]
url=${env:UNION_URL}
client=${env:UNION_CLIENT}
region=${env:UNION_REGION}
asset=${env:UNION_ASSET}
folder=${env:UNION_FOLDER}

[local]
reader.folder=${env:READER_FOLDER}
